// Templates saved out of the editor ("Save as template" in the Pages dialogue).
//
// Unlike the built-in templates in ../page-templates.js — which are lists of section keys,
// so their copy always comes from each type's own defaults — these carry the content they
// were saved with, images included. That is the whole point of them: a page someone spent
// an afternoon writing, reusable as a starting point.
//
// Adding one is two steps, because a browser cannot enumerate a directory and this project
// has no build step to generate a manifest:
//
//   1. drop the downloaded file in this folder
//   2. import it below and list it in SAVED
//
// Empty is the normal state — nothing here is required for the editor to run.

/** @type {import('../page-templates.js').PageTemplate[]} */
export const SAVED = [
  // import jfkToLhr from './jfk-to-lhr.js';   →   jfkToLhr,
];
